// Sirius landing — pricing reference intentionally disabled.
// The live Sirius landing page is waitlist-only, so this reference file exports
// a no-op instead of a pricing section.

const LPPricing = () => null;

Object.assign(window, { LPPricing });
